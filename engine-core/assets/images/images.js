import { LandingBackgroundSpriteData } from './menu-items/landing-background.js';

const Images = () => {
  return ({
    "images":{
      //"landingBackground": LandingBackgroundSpriteData(),
      "landingBackground": {
        "idle0": 'menu-items/landing-background.png'
      }
    }
  })
}

export { Images };
